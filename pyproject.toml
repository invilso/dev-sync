[project]
name = "dev-sync"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.1.1",
    "watchdog>=6.0.0",
    "websockets>=15.0.1",
]
